[project]
name = "unified-sources"
version = "0.1.1"
description = "DLT sources for Unified Reporting"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dlt[duckdb]>=1.11.0",
]

[dependency-groups]
seznam-sklik = [
    "sklik>=1.0.5",
]
