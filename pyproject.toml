[project]
name = "unified-sources"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dlt[duckdb]>=1.11.0",
]

[dependency-groups]
seznam-sklik = [
    "sklik>=1.0.5",
]
